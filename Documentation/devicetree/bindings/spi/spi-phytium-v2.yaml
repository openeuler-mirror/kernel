# SPDX-License-Identifier: (GPL-2.0 OR BSD-2-Clause)
%YAML 1.2
---
title: Phytium SPI-V2 controller

maintainers:
  - Peng Min <pengmin1540@phytium.com.cn>

allOf:
  - $ref: spi-v2-controller.yaml#

properties:
  compatible:
    const: phytium,spi-2.0

  reg:
    minItems: 1
    description: address and length of the sharememory and regfile

  interrupts:
    maxItems: 1
    description: should contain one interrupt

  clocks:
    maxItems: 1
    description: spi clock phandle

required:
  - compatible
  - "#address-cells"
  - "#size-cells"
  - reg
  - interrupts
  - clocks
  - num-cs

examples:
  - |

    spi0: spi@2800c000 {
      compatible = "phytium,spi-2.0";
      interrupts = <GIC_SPI 18 IRQ_TYPE_LEVEL_HIGH>;
      reg = <0x0 0x27014000 0x0 0x1000 0x0 0x27007000 0x0 0x1000>;
      clocks = <&sysclk_48mhz>;
      num-cs = <4>;
    };
